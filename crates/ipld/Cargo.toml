[package]
name = "bolt-ipld"
description = "IPLD encoding for Boltchain blocks: eth-block (0x90) headers whose CID equals the block hash, 1 MiB raw body chunks, dag-cbor envelopes and epoch indexes, CAR import/export"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]

[lints]
workspace = true
