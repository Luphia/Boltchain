[package]
name = "bolt-sim"
description = "Deterministic multi-node simulator with latency, partitions and Byzantine behaviour, used to check consensus safety and liveness"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]

[lints]
workspace = true
