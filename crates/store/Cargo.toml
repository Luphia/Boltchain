[package]
name = "bolt-store"
description = "Local storage on libmdbx: flat state, MPT intermediate nodes, 128-block changesets, headers/QCs, the IPFS blockstore and the slashing-protection database"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]

[lints]
workspace = true
